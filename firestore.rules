
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Prototyping rules: Allowing full read/write access to unblock development as requested.
    // This removes the need for auth checks or session validation during the prototype phase.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
