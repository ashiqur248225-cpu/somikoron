
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FORCE FULL OPEN ACCESS FOR PROTOTYPING
    // REFRESH: 2024-05-21-11 (Forcing rules redeploy to fix permissions error)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
